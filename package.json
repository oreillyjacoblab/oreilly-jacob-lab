{
  "name": "oreilly-jacob-lab",
  "version": "1.0.0",
  "private": true,
  "description": "O'Reilly-Jacob Lab research website with a single-source CV that generates a matching PDF download.",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "validate": "node scripts/validate.js",
    "build": "node scripts/build-cv.js",
    "build:pages": "node scripts/generate-pages.js",
    "build:thumbnails": "node scripts/generate-thumbnails.js",
    "build:pdf": "node scripts/generate-pdf.js",
    "audit:cms": "node scripts/audit-pages-yml.js",
    "test": "node scripts/test.js && npm run audit:cms"
  },
  "dependencies": {
    "@sparticuz/chromium": "^149.0.0",
    "js-yaml": "^5.2.2",
    "puppeteer": "^23.9.0",
    "puppeteer-core": "^25.3.0"
  }
}
